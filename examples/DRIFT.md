# Example usage

```sh
lockstep scan ~/Developer --policy examples/lockstep.config.json --format markdown --output DRIFT.md
```

Use the resulting report as a release-room checklist. Fix required-script errors first, then decide which warnings are policy exceptions.
