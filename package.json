{
  "name": "@rogerchappel/lockstep",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-package script and toolchain drift checker.",
  "type": "module",
  "bin": {
    "lockstep": "./dist/src/cli.js"
  },
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "files": [
    "dist/src",
    "examples",
    "README.md",
    "LICENSE",
    "SECURITY.md",
    "docs",
    "SAFETY.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md",
    "CODE_OF_CONDUCT.md"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "smoke": "npm run build && node dist/src/cli.js scan tests/fixtures/workspace --policy tests/fixtures/workspace/lockstep.config.json --format markdown --output /tmp/lockstep-smoke.md && test -s /tmp/lockstep-smoke.md",
    "package:smoke": "bash scripts/package-smoke.sh",
    "release:identity": "node scripts/check-release-identity.mjs",
    "release:check": "npm run release:identity && npm test && npm run check && npm run build && npm run smoke && npm run package:smoke",
    "prepack": "npm run build"
  },
  "keywords": [
    "cli",
    "tooling",
    "monorepo",
    "drift",
    "package-json",
    "local-first"
  ],
  "author": "Roger Chappel",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rogerchappel/lockstep.git"
  },
  "bugs": {
    "url": "https://github.com/rogerchappel/lockstep/issues"
  },
  "homepage": "https://github.com/rogerchappel/lockstep#readme",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^7.0.2"
  }
}
